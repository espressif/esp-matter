## IDF Component Manager Manifest File
dependencies:
  espressif/esp_rcp_update:
    version: "~1.2.0"
    rules:
      - if: "idf_version >=5.0"
  espressif/esp-serial-flasher:
    version: "~0.0.0"
    rules:
      - if: "idf_version >=5.0"
